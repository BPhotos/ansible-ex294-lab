EX01: inventario playbook handler idempotencia
==============================================

Tienes un inventario con estos servidores:
	web01
	web02
	db01
Y quieres conseguir el siguiente estado:

Requisitos
	1. Inventario:
		Crea un inventario llamado hosts con:
			web01 y web02 dentro del grupo web
			db01 dentro del grupo db
	2. Playbook
		Crea site.yml
		El playbook debe:
			Ejecutarse únicamente sobre el grupo web.
			Utilizar become.
			Instalar nginx.
			Asegurarse de que el servicio nginx:
				está arrancado
				está habilitado para arrancar automáticamente con el sistema.
	3. Página web
		Crea una página /var/www/html/index.html.
		El contenido debe mostrar:
			"Servidor: web01" en web01.
			"Servidor: web02" en web02.
		No escribas dos tareas distintas para cada servidor. Utiliza una variable o un fact.

	4. Handler:
		Si modificas index.html, nginx debe reiniciarse mediante un handler.
		No queremos reiniciar nginx si el fichero no ha cambiado.
	
	5. Idempotencia
		Ejecuta: ansible-playbook -i hosts site.yml dos veces.
		La primera ejecución debe realizar cambios.
		La segunda debería mostrar algo parecido a: changed=0 en los servidores.

	6. Restricciones:
		Quiero que lo hagas desde cero.
		No necesitas utilizar:
		roles
		Vault
		Collections externas
		Jinja2 avanzado
		Sí quiero que utilices FQCN, por ejemplo: ansible.builtin.copy en lugar de: copy

EX02: 
Partimos del ejercicio 1 y mantenemos la misma infraestructura.
1. CREAR GROUP_VARS
===================

Crear la siguiente estructura:

group_vars/
└── web.yml

Dentro de web.yml debe existir una variable llamada:

web_message

Valor requerido:

Servidor gestionado por Ansible


2. MODIFICAR EL PLAYBOOK
========================

Modificar site.yml para que la pagina web genere dinamicamente:

En lab1:

Servidor: lab1
Servidor gestionado por Ansible

En lab2:

Servidor: lab2
Servidor gestionado por Ansible

La pagina debe utilizar:

- ansible_hostname
- web_message


3. MANTENER LO APRENDIDO EN EX01
================================

El playbook debe seguir incluyendo:

[ ] hosts: web
[ ] become
[ ] FQCN
[ ] Instalacion de nginx
[ ] nginx arrancado
[ ] nginx habilitado
[ ] ansible.builtin.copy
[ ] Handler
[ ] notify
[ ] Idempotencia


4. COMPROBAR LAS VARIABLES
==========================

Antes de ejecutar el playbook, comprobar que Ansible carga la variable.

Comandos:

ansible-inventory -i hosts --graph

ansible-inventory -i hosts --list

Tambien se puede comprobar directamente:

ansible web -i hosts -m ansible.builtin.debug -a "var=web_message"


5. DOCUMENTACION
================

Se permite y se recomienda consultar documentacion oficial.

Buscar especificamente:

Ansible group_vars
Ansible variable precedence


6. RESTRICCIONES
================

No utilizar roles todavia.

No escribir el valor de web_message directamente en site.yml.

No crear una tarea diferente para lab1 y otra para lab2.

El playbook debe funcionar utilizando la misma logica para ambos servidores.

9. VERIFICACION
===============

Ejecutar:

ansible-playbook -i hosts site.yml

Comprobar que no hay errores.

Ejecutar el playbook una segunda vez.

Esperar:

changed=0

si los servidores ya estan en el estado deseado.


EX03 — host_vars y condiciones con when

En los ejercicios anteriores has aplicado la misma configuración a todos los servidores del grupo web.
Ahora vamos a introducir una situación habitual en una infraestructura real:
Los servidores pertenecen al mismo grupo y comparten una configuración base, pero algunos tienen características o necesidades diferentes.

Debes resolverlo utilizando:

host_vars;
variables específicas por host;
condiciones con when.
Infraestructura

Partimos del mismo inventario:

[web]
lab1
lab2

[db]
db01

Seguimos trabajando únicamente sobre:

hosts: web

Por tanto, db01 no debe verse afectado.

Requisito 1 — Crear variables por host

Crea la siguiente estructura:

host_vars/
├── lab1.yml
└── lab2.yml

Cada servidor deberá tener una variable llamada:

web_environment

Los valores serán diferentes:

lab1
production
lab2
development
Requisito 2 — Mostrar el entorno en la página

Modifica la página web para que incluya:

Servidor: <hostname real>
Servidor gestionado por Ansible
Entorno: <valor de web_environment>

Por ejemplo:

Servidor: debian
Servidor gestionado por Ansible
Entorno: production

y en el otro servidor:

Servidor: lab2
Servidor gestionado por Ansible
Entorno: development

Debes seguir utilizando:

ansible_hostname;
web_message;
web_environment.

Requisito 3 — Primera condición

Añade una tarea nueva que solo se ejecute en el servidor cuyo entorno sea production.

La tarea debe crear:

/var/www/html/production.txt

El contenido debe ser:

Este servidor es de PRODUCCION

Para decidir si la tarea se ejecuta o no debes utilizar:

when

No debes crear un playbook diferente.

No debes crear un grupo nuevo.

La decisión debe depender de la variable:

web_environment
Requisito 4 — Condición inversa

Añade otra tarea que solo se ejecute cuando el servidor NO sea de producción.

Debe crear:

/var/www/html/development.txt

con el contenido:

Este servidor NO es de PRODUCCION

También debes utilizar when.

Requisito 5 — Mantener lo aprendido

El playbook debe conservar:

hosts: web;
become: true;
FQCN;
instalación de nginx;
nginx habilitado;
nginx arrancado;
group_vars;
host_vars;
facts;
handler;
idempotencia.
Resultado esperado

Después de ejecutar el playbook:

En lab1

Debe existir:

/var/www/html/index.html
/var/www/html/production.txt

Y no debe existir:

/var/www/html/development.txt
En lab2

Debe existir:

/var/www/html/index.html
/var/www/html/development.txt

Y no debe existir:

/var/www/html/production.txt
⚠️ Pista conceptual importante

when no crea estados por sí mismo.

Si una tarea tiene:

when: web_environment == 'production'

esa tarea simplemente:

production → se ejecuta
development → se salta

Pero piensa en qué ocurriría si:

ejecutas el playbook cuando un host es production;
se crea production.txt;
cambias posteriormente su variable a development;
vuelves a ejecutar.

Este detalle será importante para entender la diferencia entre:

ejecución condicional

y:

gestión declarativa del estado

Por ahora, no tienes que resolver ese caso adicional, pero quiero que observes qué ocurre.

Comprobaciones

Antes de ejecutar el playbook, comprueba las variables.

Puedes probar algo como:

ansible web -i hosts -m ansible.builtin.debug -a "var=web_environment"

Deberías obtener valores distintos según el host.

Después ejecuta:

ansible-playbook -i hosts site.yml

Y comprueba los ficheros:

ansible web -i hosts -m ansible.builtin.command -a "ls -l /var/www/html/"
Estructura esperada al terminar
Certi/
├── apuntes.txt
├── enunciados.txt
├── hosts
├── site.yml
├── group_vars/
│   └── web.yml
└── host_vars/
    ├── lab1.yml
    └── lab2.yml
🧠 Qué debes sacar en claro del EX03

Al terminar debes entender:

group_vars
    ↓
Configuración común para un grupo

host_vars
    ↓
Configuración específica para un host

when
    ↓
Decidir si una tarea se ejecuta según una condición

La progresión hasta ahora es:

EX01 → Mismo estado para todos
EX02 → Configuración común por grupo
EX03 → Diferencias por host + ejecución condicional

EX04 — register, resultados y condiciones basadas en comandos
i==================================================================
Hasta ahora tus condiciones dependen de variables que tú defines:

host_vars
    ↓
web_environment
    ↓
when

Ahora vamos a dar un paso importante:

EJECUTAR UNA COMPROBACIÓN
          ↓
       register
          ↓
 GUARDAR EL RESULTADO
          ↓
        when
          ↓
 TOMAR UNA DECISIÓN

Esto se parece mucho más a la automatización real.

🎯 Objetivo

Crear un playbook que compruebe información real de cada servidor y tome decisiones en función del resultado obtenido.

Seguirás trabajando con:

[web]
lab1
lab2

Requisito 1 — Comprobar si existe nginx

Añade una tarea que compruebe si existe el binario:

/usr/sbin/nginx

Puedes utilizar:

ansible.builtin.command

Guarda el resultado de la ejecución utilizando:

register

La variable de resultado puede llamarse:

nginx_check
Requisito 2 — Mostrar el resultado

Utiliza:

ansible.builtin.debug

para mostrar únicamente el código de retorno del comando.

Debes acceder al resultado almacenado mediante la variable registrada.

Pista conceptual:

register
   ↓
nginx_check
   ↓
nginx_check.rc
Requisito 3 — Condición basada en el resultado

Crea una tarea que solo se ejecute si nginx existe.

La condición debe basarse en el código de retorno (rc) del comando anterior.

La tarea debe mostrar un mensaje similar a:

nginx está instalado en este servidor
Requisito 4 — Condición inversa

Crea otra tarea que solo se ejecute si nginx no existe.

Debe mostrar:

nginx NO está instalado en este servidor

La decisión debe depender del resultado registrado anteriormente.

Requisito 5 — Añadir una comprobación diferente por entorno

Aquí mezclamos EX03 y EX04.

En los hosts de:

production

realiza una comprobación adicional sobre nginx.

En los hosts que no sean production, esa comprobación no debe ejecutarse.

Puedes decidir qué comprobar, pero debe ser una comprobación real ejecutada mediante un módulo y guardar su resultado con register.

Después muestra parte del resultado utilizando debug.

🧠 Concepto central

Hasta ahora:

when: web_environment == "production"

La decisión dependía de un dato que tú habías definido.

Ahora también podrás hacer:

COMANDO REAL
      ↓
RESULTADO REAL
      ↓
register
      ↓
when
      ↓
DECISIÓN

Ejemplo conceptual:

¿Existe nginx?
       │
       ▼
    command
       │
       ▼
      rc=0
       │
       ▼
      when
       │
       ▼
 "nginx instalado"
Restricciones

Debes mantener todo lo anterior:

group_vars;
host_vars;
facts;
when;
handler;
FQCN;
idempotencia.

No necesitas crear un playbook nuevo. Puedes añadir las tareas a site.yml.

Qué debes aprender con EX04

Al terminar debes entender:

register
    ↓
guardar resultado de una tarea

variable_registrada.rc
    ↓
código de retorno

debug
    ↓
inspeccionar resultados

when
    ↓
tomar decisiones con información real

Este ejercicio ya empieza a entrar en un patrón muy habitual en automatización:

COMPROBAR
    ↓
ANALIZAR
    ↓
DECIDIR
    ↓
ACTUAR

Y ese patrón conecta bastante directamente con el troubleshooting que hiciste en la infraestructura real: comprobar SSH/Python, interpretar el resultado y separar los hosts según su comportamiento.

EX05 
EX05 — Loops (loop) y automatización repetitiva
🎯 Objetivo

Aprender a ejecutar una misma tarea sobre varios elementos sin repetir la tarea manualmente.

Hasta ahora podrías hacer algo así:

- name: Instalar curl
  ansible.builtin.apt:
    name: curl
    state: present

- name: Instalar vim
  ansible.builtin.apt:
    name: vim
    state: present

- name: Instalar tree
  ansible.builtin.apt:
    name: tree
    state: present

Funciona, pero es repetitivo.

Con loop:

UNA TAREA
    +
UNA LISTA
    ↓
REPETIR AUTOMÁTICAMENTE

Requisito 1 — Instalar varios paquetes

En tu mismo site.yml, añade una tarea para instalar estos paquetes:

curl
vim
tree

Debes utilizar:

ansible.builtin.apt

y:

loop

La estructura conceptual es:

- name: Instalar paquetes
  ansible.builtin.apt:
    name: ...
    state: present
  loop:
    - ...
    - ...
    - ...

Dentro del módulo tendrás que utilizar la variable especial:

item
Requisito 2 — Crear varios directorios

Crea los siguientes directorios:

/opt/lab
/opt/lab/logs
/opt/lab/config

Debes utilizar:

ansible.builtin.file

y:

loop

Todos deben tener:

state: directory
Requisito 3 — Crear varios archivos

Dentro de los directorios anteriores, crea estos archivos:

/opt/lab/config/app.conf
/opt/lab/config/database.conf
/opt/lab/logs/app.log

Puedes utilizar:

ansible.builtin.copy

con contenido simple.

Ejemplo conceptual:

app.conf
    ↓
Configuración de aplicación

database.conf
    ↓
Configuración de base de datos

app.log
    ↓
Log inicializado por Ansible

Aquí también debes utilizar:

loop
Requisito 4 — Loop con condiciones

Ahora vamos a combinar EX03 + EX05.

Tienes:

lab1 → producción
lab2 → Development

Instala este paquete en ambos:

htop

Pero instala este otro paquete solamente en producción:

ncdu

Puedes hacerlo con tareas separadas y when, o si quieres experimentar, intentar combinar la lógica con loops.

Requisito 5 — Loop + variables

Este es el pequeño reto del módulo.

En lugar de escribir directamente la lista de paquetes dentro de la tarea, crea una variable en:

group_vars/web.yml

Por ejemplo, conceptualmente:

web_packages:
  - curl
  - vim
  - tree

Después utiliza esa variable en el loop.

La idea es:

group_vars
      ↓
web_packages
      ↓
loop
      ↓
item
      ↓
apt
Resultado esperado

Al terminar deberías poder pensar así:

LISTA
  ↓
loop
  ↓
item 1
item 2
item 3
  ↓
MISMA TAREA EJECUTADA VARIAS VECES

Y tu automatización debería dejar ambos servidores con una estructura similar a:

/opt/lab/
├── config/
│   ├── app.conf
│   └── database.conf
└── logs/
    └── app.log

Con ncdu únicamente en:

lab1 → producción
Restricciones

Mantén todo lo que ya tienes:

group_vars
host_vars
when
register
debug
handler
idempotencia
FQCN (ansible.builtin...)
Concepto central del EX05
Antes:

TAREA 1 → curl
TAREA 2 → vim
TAREA 3 → tree


Ahora:

UNA TAREA
    ↓
loop
    ↓
curl
vim
tree

Mi recomendación: haz primero el requisito 1 y pégame solo esa tarea. No intentes resolver los cinco de golpe; este módulo es para que interiorices muy bien la relación entre loop e item.




# BLOQUE 6 — TEMPLATES Y JINJA2

## OBJETIVO

Utilizar templates de Ansible para generar archivos dinámicos utilizando variables y condiciones Jinja2.

Se utilizarán los servidores del grupo:

web

Las variables disponibles son:

ansible_hostname
web_message
web_environment

La página web se generará en:

/var/www/html/index.html

---

# BLOQUE 6 — TEMPLATES Y JINJA2

## OBJETIVO

Utilizar templates de Ansible para generar archivos dinámicos en los servidores web.

Los archivos generados deberán utilizar las variables ya existentes en el proyecto y mostrar contenido diferente dependiendo del entorno de cada servidor.

## VARIABLES DISPONIBLES

Durante el ejercicio se utilizarán las siguientes variables:

ansible_hostname

web_message

web_environment

El resultado final se generará en:

/var/www/html/index.html

==================================================
REQUISITO 1 — CREAR EL DIRECTORIO DE TEMPLATES
==============================================

Dentro del proyecto de Ansible crea un directorio llamado:

templates/

La estructura del proyecto deberá quedar similar a:

Certi/
│
├── hosts
├── site.yml
│
├── group_vars/
│   └── web.yml
│
├── host_vars/
│   ├── lab1.yml
│   └── lab2.yml
│
└── templates/
└── index.html.j2

==================================================
REQUISITO 2 — CREAR EL TEMPLATE
===============================

Dentro del directorio:

templates/

Crea un archivo llamado:

index.html.j2

El template deberá utilizar las siguientes variables:

ansible_hostname

web_message

web_environment

El resultado generado deberá mostrar información similar a:

Servidor: nombre_del_servidor

mensaje: mensaje_definido_en_la_variable

Entorno: entorno_del_servidor

==================================================
REQUISITO 3 — UTILIZAR VARIABLES DE ANSIBLE
===========================================

El template deberá utilizar variables de Ansible mediante la sintaxis Jinja2:

{{ variable }}

Como mínimo deberán utilizarse:

{{ ansible_hostname }}

{{ web_message }}

{{ web_environment }}

El mismo template deberá generar contenido diferente dependiendo de las variables de cada servidor.

==================================================
REQUISITO 4 — DESPLEGAR EL TEMPLATE
===================================

Modifica el playbook:

site.yml

para generar la página web utilizando:

ansible.builtin.template

El template:

index.html.j2

deberá generarse en:

/var/www/html/index.html

La tarea deberá notificar al handler de nginx cuando el archivo sea modificado.

El flujo esperado será:

Template
↓
ansible.builtin.template
↓
/var/www/html/index.html
↓
notify
↓
Handler reinicia nginx

==================================================
REQUISITO 5 — CONTENIDO DIFERENTE SEGÚN EL ENTORNO
==================================================

Modifica el template:

templates/index.html.j2

para generar contenido diferente dependiendo del valor de:

web_environment

La condición deberá realizarse dentro del propio template utilizando Jinja2.

Si el servidor pertenece al entorno:

producción

deberá mostrar:

ESTE SERVIDOR ES DE PRODUCCIÓN

Si el servidor pertenece a otro entorno deberá mostrar:

ESTE SERVIDOR ES DE DESARROLLO

La lógica deberá realizarse mediante una condición Jinja2.

Pista conceptual:

if
else
endif

IMPORTANTE:

Al comparar una variable con un valor de texto, el texto debe tratarse como una cadena.

Ejemplo conceptual:

web_environment == "producción"

==================================================
REQUISITO 6 — VERIFICAR EL RESULTADO GENERADO
=============================================

Después de desplegar el template, verifica desde Ansible el contenido del archivo:

/var/www/html/index.html

Para ello:

1. Ejecuta un comando desde Ansible para leer el archivo.

2. Guarda el resultado utilizando:

register

3. Evita que una tarea de comprobación aparezca como un cambio si no modifica el sistema.

4. Muestra únicamente el contenido relevante del resultado.

La comprobación deberá permitir verificar que cada servidor ha generado su propio contenido.

El resultado esperado será similar a:

## LAB1

Servidor: debian
mensaje: Servidor gestionado por Ansible
Entorno: producción
ESTE SERVIDOR ES DE PRODUCCIÓN

## LAB2

Servidor: lab2
mensaje: Servidor gestionado por Ansible
Entorno: Development
ESTE SERVIDOR ES DE DESARROLLO

==================================================
RESULTADO FINAL ESPERADO
========================

El ejercicio completo deberá demostrar:

Variables
+
Templates
+
Jinja2
+
Condiciones
+
ansible.builtin.template
+
notify
+
handlers
+
command
+
register
+
changed_when
+
debug

## FLUJO FINAL

Variables del inventario
↓
Template Jinja2
↓
Evaluar el entorno
↓
Generar contenido diferente
↓
Crear index.html en cada servidor
↓
Reiniciar nginx si hubo cambios
↓
Verificar el archivo generado
↓
Guardar resultado
↓
Mostrar únicamente la información relevante





# BLOQUE 7 — ROLES EN ANSIBLE

## OBJETIVO

Aprender a organizar un proyecto de Ansible utilizando Roles.

Hasta ahora, la mayor parte de las tareas se encuentran dentro de:

site.yml

El objetivo de este bloque es separar la configuración relacionada con el servidor web dentro de un Role.

El concepto principal es:

site.yml
↓
Orquesta qué se ejecuta

Role
↓
Contiene la implementación de una responsabilidad concreta

En este bloque se creará un Role llamado:

web

==================================================
REQUISITO 1 — CREAR EL ROLE
===========================

Dentro del proyecto de Ansible crea un Role llamado:

web

La estructura deberá contener como mínimo:

roles/
└── web/
├── tasks/
│   └── main.yml
│
├── handlers/
│   └── main.yml
│
└── templates/
└── index.html.j2

Puedes crear esta estructura manualmente o utilizar las herramientas disponibles en Ansible para generar la estructura inicial de un Role.

Objetivo conceptual:

roles/
↓
web
↓
Responsabilidad: configurar un servidor web

==================================================
REQUISITO 2 — MOVER LAS TAREAS WEB AL ROLE
==========================================

Dentro del archivo:

roles/web/tasks/main.yml

mueve las tareas necesarias para configurar el servidor web.

Como mínimo, el Role deberá encargarse de:

1. Instalar nginx.

2. Habilitar nginx para que arranque automáticamente.

3. Arrancar nginx.

4. Generar la página web:

/var/www/html/index.html

La página deberá generarse utilizando un template.

IMPORTANTE:

El archivo:

roles/web/tasks/main.yml

contiene directamente las tareas del Role.

No es necesario definir dentro de él un Play completo.

El objetivo conceptual es:

Role web
↓
Instalar nginx
↓
Configurar nginx
↓
Generar página web

==================================================
REQUISITO 3 — MOVER EL HANDLER AL ROLE
======================================

El handler encargado de reiniciar nginx deberá moverse desde:

site.yml

a:

roles/web/handlers/main.yml

El handler deberá continuar realizando la misma función:

reiniciar nginx

La tarea que genera el template deberá continuar notificando al handler cuando el archivo cambie.

IMPORTANTE:

El archivo:

roles/web/handlers/main.yml

contiene directamente los handlers del Role.

==================================================
REQUISITO 4 — MOVER EL TEMPLATE AL ROLE
=======================================

Mueve el template:

index.html.j2

desde su ubicación anterior al directorio:

roles/web/templates/

La estructura deberá quedar:

roles/web/
├── tasks/
├── handlers/
└── templates/
└── index.html.j2

La tarea encargada de generar la página deberá utilizar el template desde la estructura del Role.

Comprueba que:

lab1

sigue generando contenido correspondiente al entorno de producción.

Y que:

lab2

sigue generando contenido correspondiente al entorno de desarrollo.

==================================================
REQUISITO 5 — SIMPLIFICAR SITE.YML
==================================

Modifica:

site.yml

para utilizar el Role:

web

El Playbook deberá seguir definiendo:

* Nombre del Play.
* Hosts.
* Privilegios necesarios.
* Role que se debe aplicar.

La responsabilidad del:

site.yml

será principalmente orquestar qué Role se ejecuta sobre qué servidores.

Conceptualmente:

site.yml
↓
Aplicar Role web
↓
Servidores del grupo web

==================================================
REQUISITO 6 — MANTENER COMPROBACIONES EN SITE.YML
=================================================

Las tareas de comprobación y ejercicios anteriores pueden continuar existiendo dentro de:

site.yml

junto con la ejecución del Role.

La estructura conceptual será:

site.yml
│
├── Configuración del Play
│
├── Roles
│   └── web
│
└── Tasks
├── Comprobaciones
├── Register
├── Debug
├── When
└── Otros ejercicios

El objetivo es comprender que:

Roles
y
Tasks directas en un Playbook

pueden convivir.

==================================================
REQUISITO 7 — EJECUTAR Y VALIDAR
================================

Ejecuta el Playbook sobre los servidores definidos en:

hosts

Comprueba que:

1. nginx está instalado.

2. nginx está habilitado.

3. nginx está arrancado.

4. La página web se genera correctamente.

5. El contenido generado es diferente dependiendo del entorno.

6. El handler se ejecuta cuando existen cambios.

7. Las comprobaciones existentes en el Playbook continúan funcionando.

También deberás comprobar que no existen tareas duplicadas entre:

site.yml

y:

roles/web/tasks/main.yml

==================================================
RESULTADO FINAL ESPERADO
========================

La estructura del proyecto deberá separar claramente:

ORQUESTACIÓN
↓
site.yml

RESPONSABILIDAD WEB
↓
roles/web/

El Role web contendrá:

roles/web/
│
├── tasks/
│   └── main.yml
│
├── handlers/
│   └── main.yml
│
└── templates/
└── index.html.j2

El Playbook principal será el encargado de indicar:

Qué servidores
↓
Qué privilegios
↓
Qué Role ejecutar
↓
Qué comprobaciones adicionales realizar

==================================================
CONCEPTO PRINCIPAL DEL BLOQUE
=============================

Un Role encapsula una responsabilidad.

Ejemplo:

Role web

Responsabilidad:

Configurar un servidor web.

El Role contiene la implementación.

El Playbook decide dónde se aplica.

Flujo:

site.yml
↓
Selecciona servidores
↓
Aplica Role web
↓
Role ejecuta tareas
↓
Role utiliza templates
↓
Role puede utilizar handlers
↓
Servidor configurado

## OBJETIVO DE APRENDIZAJE

Al finalizar este bloque debes ser capaz de diferenciar entre:

Playbook
↓
Orquestación

Role
↓
Organización y reutilización de una responsabilidad

Tasks
↓
Acciones a ejecutar

Handlers
↓
Acciones desencadenadas por cambios

Templates
↓
Archivos dinámicos generados mediante variables y Jinja2

